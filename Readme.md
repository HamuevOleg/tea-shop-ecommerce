# Tea Shop Ecommerce

## Описание проекта

**Tea Shop Ecommerce** — это современный интернет-магазин чая и чайных аксессуаров. Это Fullstack-приложение, которое позволяет пользователям выбирать товары, формировать корзину и оформлять заказы, а администраторам — управлять каталогом и статусами заказов.

### Реализованный функционал:

**Для пользователей:**

* 🔐 **Авторизация:** Регистрация и вход (JWT).
* 🍵 **Каталог:** Просмотр товаров с фильтрацией по категориям.
* 🛒 **Корзина:** Добавление товаров, изменение количества, расчет стоимости.
* 📦 **Заказы:** Оформление заказа и сохранение истории.
* 👤 **Профиль:** Просмотр своих данных.

**Для администраторов:**

* ⚙️ **Управление товарами:** Добавление, редактирование, удаление.
* 🗂 **Управление категориями:** Создание и редактирование категорий чая.
* 📋 **Управление заказами:** Смена статусов заказов пользователей.

**Технологический стек:**

* **Frontend:** React, TypeScript, Vite, Tailwind (или CSS модули).
* **Backend:** ElysiaJS (на базе Bun), Prisma ORM.
* **База данных:** PostgreSQL.
* **Среда выполнения:** Bun.
* **Контейнеризация:** Docker (для БД).

-----

## 🛠 Предварительная установка (Prerequisites)

Перед запуском убедитесь, что у вас установлены необходимые инструменты.

### 1\. Установка Bun (Package Manager & Runtime)

**Для macOS:**
Откройте терминал и выполните:

```bash
curl -fsSL https://bun.sh/install | bash
```

**Для Windows:**
Откройте PowerShell (от имени администратора) и выполните:

```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

### 2\. Установка Docker Desktop

Docker необходим для запуска базы данных PostgreSQL.

* **Скачать для Windows/Mac:** [Официальный сайт Docker](https://www.docker.com/products/docker-desktop/)
* Установите и **запустите** приложение Docker Desktop.

-----

## 🚀 Инструкция по запуску

Клонируйте репозиторий (если еще этого не сделали):

```bash
git clone <ваша-ссылка-на-репозиторий>
cd tea-shop-ecommerce
```

### Шаг 1: Настройка Backend (Сервер и База данных)

1.  Перейдите в папку сервера:

    ```bash
    cd server
    ```

2.  Установите зависимости:

    ```bash
    bun install
    ```

3.  **Создание `.env` файла:**
    В папке `server` создайте файл с именем `.env`. Скопируйте туда следующий контент (пароли можно изменить):

    ```env
    # Порт сервера
    PORT=3000

    # Секретный ключ для JWT
    JWT_SECRET=super_secret_tea_key_change_me

    # Настройки базы данных (должны совпадать с docker-compose.yml)
    POSTGRES_USER=user
    POSTGRES_PASSWORD=password
    POSTGRES_DB=teashop

    # Строка подключения для Prisma
    DATABASE_URL="postgresql://user:password@localhost:5432/teashop?schema=public"
    ```

4.  **Запуск Базы данных:**
    В папке `server` (где лежит файл `docker-compose.yml`) выполните команду:

    ```bash
    docker-compose up -d
    ```

    *Эта команда скачает и запустит PostgreSQL в контейнере.*

5.  **Настройка схемы БД (Prisma):**
    Примените миграции, чтобы создать таблицы в базе:

    ```bash
    bunx prisma migrate dev --name init
    ```

    *(Или просто `bunx prisma db push` для прототипирования)*

6.  **Заполнение базы тестовыми данными (Seeding):**
    Чтобы добавить категории и товары (чай) в базу, выполните команду сида (скрипт лежит в `prisma/seed.ts`):

    ```bash
    bun run prisma/seed.ts
    ```

7.  **Запуск Сервера:**

    ```bash
    bun run dev
    ```

    Сервер запустится по адресу: `http://localhost:3000`.

### Шаг 2: Настройка Frontend (Клиент)

1.  Откройте **новый** терминал и перейдите в папку клиента (из корня проекта):

    ```bash
    cd client
    ```

2.  Установите зависимости:

    ```bash
    bun install
    ```

3.  **Запуск Клиента:**

    ```bash
    bun run dev
    ```

    Приложение будет доступно по адресу (обычно): `http://localhost:5173`.

-----

## 📚 Документация и Работа с БД

### Как зайти в Swagger (Документация API)

ElysiaJS автоматически генерирует Swagger документацию.

1.  Убедитесь, что сервер (`server`) запущен.
2.  Откройте в браузере:
    **[http://localhost:3000/swagger](https://www.google.com/search?q=http://localhost:3000/swagger)**

### Как зайти в Prisma Studio (Просмотр БД)

Prisma Studio — это удобный графический интерфейс для просмотра и редактирования данных в базе напрямую.

1.  Откройте терминал в папке `server`.
2.  Выполните команду:
    ```bash
    bunx prisma studio
    ```
3.  В браузере откроется панель управления базой данных (обычно на `http://localhost:5555`).

-----

## ⚠️ Важные замечания

* **Файл `.env`**: Этот файл содержит секретные ключи и пароли от базы данных. Он **добавлен в .gitignore** и не должен попадать в репозиторий. Каждый разработчик создает его локально.
* **Перезапуск**: Если вы перезагружаете компьютер, не забудьте снова запустить Docker Desktop и контейнер с базой (`docker-compose up -d`), прежде чем запускать `bun run dev`.